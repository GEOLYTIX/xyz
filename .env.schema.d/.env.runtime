# XYZ runtime configuration.
# @defaultRequired=false @defaultSensitive=false

# @type=number(min=1)
COOKIE_TTL=36000

# XYZ API path prefix.
# @type=string
DIR=

# @type=number(min=1)
FAILED_ATTEMPTS=3

# @type=port
PORT=3000

# @type=number(min=1)
RATE_LIMIT=1000

# @type=number(min=1)
RATE_LIMIT_WINDOW=60000

# @type=number(min=1)
RETRY_LIMIT=3

# @type=enum(HS256, HS384, HS512, RS256, RS384, RS512, ES256, ES384, ES512)
SECRET_ALGORITHM=HS256

# @type=string
TITLE=GEOLYTIX | XYZ

# @type=number(min=1)
WORKSPACE_AGE=3600000

# @type=string
FILE_RESOURCES=resources

# @type=string
PASSWORD_REGEXP=(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])^.{10,}$

# @type=number(min=1)
STATEMENT_TIMEOUT=
