# XYZ environment schema for Varlock.
# Local secrets should stay in .env.local, which Varlock auto-loads and remains gitignored.
# Deployments can import only the partial schemas they need from .env.schema.d/.
# @currentEnv=$VERCEL_ENV
# @defaultRequired=false @defaultSensitive=false
# @import(./.env.schema.d/.env.runtime)
# @import(./.env.schema.d/.env.auth)
# @import(./.env.schema.d/.env.data)
# @import(./.env.schema.d/.env.integrations)
# @import(./.env.schema.d/.env.workspace)
# @import(./.env.schema.d/.env.saml)
# @import(./.env.schema.d/.env.gsm)
# ---

# Vercel deployment environment: production, preview, or development.
# @type=string
VERCEL_ENV=
