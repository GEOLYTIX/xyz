# @defaultRequired=false
# @defaultSensitive=true
# @currentEnv=$APP_ENV
# ---

# Keep platform variables such as NODE_ENV, VERCEL, and XYZ_CWD out of the
# schema so Varlock does not override them at runtime.
# @sensitive=false
APP_ENV=development

# @sensitive=false
TITLE=

# @sensitive=false
WORKSPACE=

SECRET=
