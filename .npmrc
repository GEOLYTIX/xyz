# pnpm configuration
# Use strict peer dependencies to ensure compatibility
strict-peer-dependencies=true

# Enable hoisting for better compatibility with tools that expect node_modules structure
hoist-pattern[]=*

# Auto install peers to reduce manual dependency management
auto-install-peers=true

# Use the node_modules symlink structure for better tool compatibility
node-linker=hoisted

# Registry configuration (use default npm registry)
registry=https://registry.npmjs.org/

# Save exact versions by default for reproducible builds
save-exact=true
